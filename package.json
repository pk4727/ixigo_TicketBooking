{
  "name": "ixigo",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "clean:reports": "rimraf test-results",
    "clean:videos": "rimraf videos",
    "clean:screenshots": "rimraf screenshots",
    "clean:all": "npm run clean:reports && npm run clean:videos && npm run clean:screenshots",
    "pretest":"npm install && ./.bin/playwright.cmd install --force && npm run clean:all",
    "install": "npx playwright install",
    "headed": "npx playwright test --headed",
    "headless": "npx playwright test",
    "UI": "npx playwright test --ui",
    "individual file": "npx playwright test example"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.55.1",
    "@types/node": "^24.6.2"
  }
}